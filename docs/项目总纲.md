# 项目总纲

工行杯 · 金融安全服务方向 · 票据不说谎

本文是 PPT 的骨架，也是判断"做到哪了"的依据。
**已验证**的写数字，**待验证**的明确标注，不混在一起。

---

## 一、目的

### 要解决的问题

票据是资金流出的起点。企业报销、商户对账、个人记账，第一步都是把纸质票据上的
数字读进系统。这一步现在有两个缺口：

1. **识别错误是静默的。** OCR 只做文字识别，不判断票据自身的算术是否成立。
   读错一个数字后，输出的 JSON 格式完好、数量级合理，不对照答案看不出来。
2. **识别错误与人为篡改无法区分。** 系统看到一个数字，无从判断它是识别偏差
   还是有人改过票据。报销风控真正关心的是后者。

### 一个亲历的失败，决定了整个设计

早期版本把算术约束写进提示词，要求模型保证 `商品行 − 折扣 = 小计`。

结果：模型读错一个数字后，**为满足这条约束又改写了第二个数字**，产出一份自洽
但错误的结果。所有一致性检查都通过，错误完全隐形。

这不是模型不听话，是约束下的最优解——给它一个必须满足的等式，凑平比读准更容易。

由此确立铁律：**所有校验只在代码侧执行，从不写入提示词。**
模型永远不知道自己正在被什么标准检查，也就无法朝那个标准编数字。

---

## 二、框架

```
票据图像
   ↓
[大模型]  只做逐行转写。明确禁止加减、核对、调平任何总额
   ↓
JSON：商品行 / 折扣行（含标签） / 小计 / 舍入 / 付款行
   ↓
[Python]  所有求和在此完成
   ↓
多条相互独立的算术约束交叉验证
   ↓
[多次识别投票]  消除单次识别的随机误差
   ↓
结构化账本 + 核验报告（标出存疑票据及失败的是哪一条）
```

### 四条校验

| | 约束 | 单独能抓到什么 |
|---|---|---|
| 一 | 应付 = 小计 − 小计**下方**的折扣 + 税费 + 服务费 + 舍入 | 应付金额被改 |
| 二 | 商品行总额 − 小计**上方**的折扣 = 小计 | 单品价格被改，且应付仍闭合 |
| 三 | 折扣标签自带金额 = 金额栏 | 折扣金额读错（港式票据专有） |
| 四 | 付款 − 找零 = 应付 | 付款行或应付行被单独改动 |

**为什么不止一条。** 校验一看不见折扣和商品行；校验二看不见应付；
校验三只在标签带金额时可用；校验四只在票面同时印了应付与付款行时可用。
已验证的两个港式篡改样本正好各被一条抓到：付款行改动只有校验一发现，单品改动只有校验二发现。

**折扣算在小计之前还是之后，看它印在哪。** 港式票据的折扣印在商品区，SUBTOTAL 已经扣过；
CORD 的整单折扣印在小计下方，还没扣；CORD 的单品折扣又印在小计上方、已经扣过。
按地区一刀切必错一头，所以让模型照抄每条折扣的位置——这是版面事实，不涉及计算。

**校验三是一个免费的校验位。** 港式超市折扣行常把金额写进标签本身
（`Buy 3 Save $9.8`、`MB APP UPGRADE -$10`），一个数字在同一行印了两遍。
模型只被要求照抄标签文字，不知道它正被用来交叉验证。

---

## 三、流程

### 运行时流程（产品）

1. 上传票据照片
2. 每张独立识别 N 次，全部请求打包进一次并发批处理
3. 每次识别在 Python 侧求和，算出各条校验的差额
4. 按证据强弱排序：自洽的优先，标签印证多的优先，最后多数投票
5. 输出账本 + 核验报告

### 验证流程（实验）

| 实验 | 数据 | 测什么 | 状态 |
|---|---|---|---|
| E0 金额归一化 | 构造用例 | 跨地区金额写法能否正确解析 | **已完成 18/18** |
| E1 标注自洽率 | CORD 34 张 | 公开票据本身有多少是自洽的（误报基线） | **已完成** |
| E2 抽取准确率 | CORD 自洽子集 | 模型读出的字段与标注是否一致 | 第一轮已跑，见下 |
| E3 篡改检出 | CORD 自洽子集 | TPR / FPR | 第一轮已跑，见下 |
| E4 定性演示 | 港式票据 7+2 张 | 篡改视觉上不明显，算术躲不掉 | FPR 0/7；两张篡改都被模型抹平（见 E5） |
| E5 转写方式对比 | 港式篡改 2 张 | 整票 / 整票+告知 / 单行裁剪 | 单行裁剪 4/4 照抄，整票被抹平 |

E3 的篡改用两种方式：
- **整块重绘**，按框高匹配字号取背景墨色，全自动跑 100 张，出统计量
- **字形复制**，从同票取同字体数字合成，视觉真实，只做 3 张港式票据当配图

---

## 四、产出

### 代码

| 文件 | 作用 | 验证状态 |
|---|---|---|
| `src/money.py` | 金额归一化，处理 `.` / `,` 千位分隔符歧义 | 18/18 离线测试通过 |
| `src/cord.py` | CORD 标注解析（兼容 menu 的 dict/list）+ 两条校验 | 34 条真实标注实测 |
| `tests/test_money_and_cord.py` | 离线测试，不需要 API key | 通过 |
| `data/hk/` | 7 张港式票据 + 2 张篡改样本 | 篡改样本已生成 |
| `src/tamper.py` | 基于 `quad` 框的图像改动 | 目标定位 199/199 |
| `src/receipt.py` | 四条校验、跨次投票 | 22/22 离线测试，含 E3 第一轮的全部失误样例 |
| `notebooks/` | Colab 跑 E1–E4 | 已写，拉取本仓库运行 |

### 数据

- **CORD-v2**（NAVER CLOVA，Donut 论文基准），公开 1000 张，标注含商品行、
  小计、折扣、税费、总额
- 港式超市票据 7 张，含逐张人工核对的真值

### 已测得的数字

```
金额归一化：18/18

CORD 公开标注实测（34 张）
  menu 为 dict / list：12 / 22
  校验一 小计+税费->总额   适用 21 张，自洽 20 张
  校验二 商品行->小计       适用 22 张，自洽 19 张
  两条都适用 21 张，都自洽 17 张
```

### E2 / E3 第一轮（commit `2fbf8ae`，20 张，每张识别 3 次）

```
E2  subtotal 19/20   实付 17/20   商品行合计 14/20
E3  被篡改 10 张检出 7   TPR 70%
    未篡改 10 张误报 2   FPR 20%
```

这一轮的价值在于暴露了**尺子本身**的三个问题，不是模型的问题：

1. **FPR 的 2 张全部来自公式缺陷。** CORD 31、33 的单品折扣印在小计上方、已计入小计，
   公式按"印尼折扣在小计之后"又扣了一次。拿标注构造逐字无误的转写，离线同样复现这 2 张。
   已改为按折扣的印刷位置区分。
2. **篡改位置决定检出：商品行 / 小计 / 税额 6/6，付款区 1/4。** 漏检的 3 张里，
   CORD 10 改的是 CASH，旧校验没有一条覆盖付款行——已加校验四；
   CORD 3、5 改的是 TOTAL，下一行 CASH 仍印原值，旧格式只有一个"实付"栏且允许填 CASH，
   无法分辨模型是照抄了未改的 CASH 还是把 TOTAL 抹平了——已拆成应付 / 付款 / 找零三栏。
3. **E2 的失分里混着口径问题。** 提示词把 CASH 列为实付、把服务费同时列进商品行和服务费栏。已修正。

### E2 / E3 第二轮（commit `e01065b`，同 20 张、同篡改目标）

```
E1  CORD test 100 张：一、二都适用 64 张，全部自洽 53 张
E2  小计 18/20   应付 19/20   商品行合计 18/20
E3  TPR 9/10 = 90%    FPR 0/10 = 0%
```

- 付款区篡改 4/4 检出，第一轮的漏检确是格式问题
- 唯一漏检 CORD 34：3 次里 2 次单独判存疑，"自洽优先"的投票选了自洽的那 1 次
- CORD 20（未篡改）：一次误读后，应付被改写成与误读一致的数，并编出票面不存在的
  付款与找零行，整份读数四条校验全过；投票采用了这一次。详见 README

**待定**：投票规则是否改为"多数单次判定"；最终数字在未参与调规则的 33 张上另跑。

---

## 五、意义

### 对金融安全方向

赛道要的是"用技术守护资金安全"，落点在**多模态模型检测**与**数字防火墙**。
本作品把凭证真伪做成资金流出的第一道关口：不是判断图像有没有被 PS，
而是判断票面数字自己讲不讲得通。

### 技术上的三点

**一、校验不进提示词。** 这是从一次真实失败里换来的。凡是告诉模型的约束，
模型就有动力去满足它而不是读准——约束必须在模型之外执行。

**二、冗余信息是免费的校验位。** 票据为了人看得懂，本来就重复记录了同一笔钱：
小计与商品行、标签与金额栏。人眼忽略的冗余，正是机器可用的交叉验证。

**三、诚实报告误报。** 公开数据上已经测出约 19% 的票据本身就不自洽
（voucher 记法、商品行未标全）。实验必须同时报 TPR 和 FPR。
只报检出率的作品，评委一问就穿。

### 固有边界

查的是**票面算术是否自洽，不是像素取证**。造假者若把商品行、小计、付款行
一并改成彼此吻合的数字，各条校验都会通过。

本方法提高的是伪造成本：改一个数字要同时骗过多条相互独立的约束，
远难于改动单处。这个边界要主动写进 PPT，不等评委问。
